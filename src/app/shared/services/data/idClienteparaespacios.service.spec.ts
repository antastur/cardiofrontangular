/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { IdClienteparaespaciosService } from './idClienteparaespacios.service';

describe('Service: IdClienteparaespacios', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [IdClienteparaespaciosService]
    });
  });

  it('should ...', inject([IdClienteparaespaciosService], (service: IdClienteparaespaciosService) => {
    expect(service).toBeTruthy();
  }));
});
